version: 0.2

batch:
  fast-fail: true
  build-list:
    - identifier: test
      buildspec: buildspec-branch-test.yml
      ignore-failure: false
    - identifier: build
      buildspec: buildspec-branch-build.yml
      ignore-failure: true
